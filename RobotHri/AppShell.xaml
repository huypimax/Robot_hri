<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:RobotHri.Views"
       x:Class="RobotHri.AppShell"
       FlyoutBehavior="Disabled"
       Shell.NavBarIsVisible="False"
       Shell.TabBarIsVisible="False">

    <Shell.Resources>
        <ResourceDictionary>
            <Style TargetType="Shell" ApplyToDerivedTypes="True">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource PrimaryDarkBlue}" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.NavBarHasShadow" Value="False" />
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <FlyoutItem FlyoutDisplayOptions="AsMultipleItems">

        <Tab Title="Main" Route="main">
            <ShellContent ContentTemplate="{DataTemplate views:MainPage}" />
        </Tab>

        <Tab Title="Q&amp;A" Route="qna">
            <ShellContent ContentTemplate="{DataTemplate views:QnaPage}" />
        </Tab>

        <Tab Title="Procedure" Route="procedure">
            <ShellContent ContentTemplate="{DataTemplate views:ProcedurePage}" />
        </Tab>
        
        <Tab Title="Procedure Detail" Route="proceduredetail">
            <ShellContent ContentTemplate="{DataTemplate views:ProcedureDetailPage}" />
        </Tab>
        
        <Tab Title="Procedure List" Route="procedurelist">
            <ShellContent ContentTemplate="{DataTemplate views:ProcedureListPage}" />
        </Tab>

        <Tab Title="Navigation" Route="navi">
            <ShellContent ContentTemplate="{DataTemplate views:NaviPage}" />
        </Tab>

        <Tab Title="Lab" Route="lab">
            <ShellContent ContentTemplate="{DataTemplate views:LabPage}" />
        </Tab>

        <Tab Title="Delivery" Route="deli">
            <ShellContent ContentTemplate="{DataTemplate views:DeliPage}" />
        </Tab>

        <Tab Title="Setup" Route="setup">
            <ShellContent ContentTemplate="{DataTemplate views:SetupPage}" />
        </Tab>

    </FlyoutItem>
</Shell>
